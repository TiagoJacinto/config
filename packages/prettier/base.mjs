/** @satisfies {import('prettier').Options} */
const config = {
  singleQuote: true,
  jsxSingleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  endOfLine: 'auto',
};

export default config;
